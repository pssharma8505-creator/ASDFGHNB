import React from "react";

export const ThemeSwitcher: React.FC<{ className?: string; compact?: boolean }> = () => {
  // Theme switching disabled - website strictly uses Snow-White design system
  return null;
};
